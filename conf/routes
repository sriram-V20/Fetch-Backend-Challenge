# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

#FETCH_BACKEND_CHALLENGE APIS
POST    /receipts/process           controllers.ReceiptProcessorController.processReceipt(request: Request)
GET     /receipts/:id/points        controllers.ReceiptProcessorController.getPoints(request: Request,id: String)